<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender for development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- Package-specific logging levels -->
    <logger name="com.kalix.ide.interaction" level="DEBUG" />
    <logger name="com.kalix.ide.cli" level="DEBUG" />
    <logger name="com.kalix.ide.managers.FileWatcherManager" level="DEBUG" />

    <!-- Reduce noise from external libraries -->
    <logger name="com.formdev.flatlaf" level="WARN" />
    <logger name="org.fife.ui" level="WARN" />
</configuration>